import { ZDurationPipe } from './z-duration.pipe';

describe('ZDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ZDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
