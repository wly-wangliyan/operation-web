import { ZCompleteNumberPipe } from './z-complete-number.pipe';

describe('ZCompleteNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ZCompleteNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
