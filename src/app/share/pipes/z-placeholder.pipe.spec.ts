import { ZPlaceholderPipe } from './z-placeholder.pipe';

describe('ZPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new ZPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
