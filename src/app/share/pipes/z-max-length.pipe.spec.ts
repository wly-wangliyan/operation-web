import { ZMaxLengthPipe } from './z-max-length.pipe';

describe('ZMaxLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new ZMaxLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
