import { ZFormatDurationPipe } from './z-format-duration.pipe';

describe('ZFormatDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ZFormatDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
