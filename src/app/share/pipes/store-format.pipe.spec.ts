import { StoreFormatPipe } from './store-format.pipe';

describe('StoreFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
