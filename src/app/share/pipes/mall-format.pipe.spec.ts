import { MallFormatPipe } from './mall-format.pipe';

describe('MallFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MallFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
