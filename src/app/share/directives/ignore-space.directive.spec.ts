import { IgnoreSpaceDirective } from './ignore-space.directive';

describe('IgnoreSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new IgnoreSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
