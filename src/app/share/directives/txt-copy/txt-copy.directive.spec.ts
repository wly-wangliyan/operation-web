import { TxtCopyDirective } from './txt-copy.directive';

describe('TxtCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new TxtCopyDirective();
    expect(directive).toBeTruthy();
  });
});
