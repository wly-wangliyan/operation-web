###
# 本地调试阶段可以使用如下语句进行代码部署(前提条件：本地代码已经build完成,dist目录已经生成)
# 注:只用于调试阶段      by zack 2020.4.21

# 本机执行:
# image_name=docker.uucin.com/zhumengyuan/operation-web:dev && docker build -f .docker/DevDockerfile --tag $image_name --no-cache . && docker push $image_name && docker rmi $image_name

# 服务器执行:
# docker pull docker.uucin.com/zhumengyuan/operation-web:dev && docker-compose -f .docker/docker-compose-dev.yaml up -d
###

FROM nginx:alpine
COPY .docker/nginx.conf /etc/nginx/nginx.conf
COPY ./dist /app/dist